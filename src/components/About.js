import react from 'react-dom';
import ReactDOM from 'react-dom';
import '../style.css';

export default function About() {
    return (
        <div className = 'About'>
        <h1>About</h1>
        <p>
            This is a simple react app that displays my information and a button that
            will display a random quote.
        </p>
        </div>
    );
    }